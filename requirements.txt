# Pinned for reproducibility. Verify CUDA compatibility for your GPU before installing.
torch==2.4.0
transformers==4.44.2
peft==0.12.0
bitsandbytes==0.43.3
accelerate==0.33.0
datasets==2.21.0
sentence-transformers==3.0.1
pyyaml==6.0.2
tqdm==4.66.5

# Dev
pytest==8.3.2
ruff==0.6.3

# Deployment — install separately, version depends on CUDA/TRT version.
# See src/deploy/README.md
# tensorrt-llm==<pin at Phase 4>
